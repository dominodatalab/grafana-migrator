"""grafana-migrator: export dashboards, folders, alert rules, contact points,
and notification policy from a standalone Grafana instance as Grafana
Operator custom resources for an operator-managed instance, skipping
anything already present there.
"""

__version__ = "0.1.0"
